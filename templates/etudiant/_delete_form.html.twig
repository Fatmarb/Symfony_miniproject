<form method="post" action="{{ path('app_etudiant_delete', {'NCE': etudiant.NCE}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etudiant.NCE) }}">
    <button class="btn">Delete</button>
</form>
